import { Button } from "@/components/ui/button";
import Image from "next/image";
const Page=() => {
    return (
        <div>
          <Button variant="new">
            Click Me
          </Button>
          </div>
    );
};
export default Page;